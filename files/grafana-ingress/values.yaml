# Default values for ingress.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
nameOverride: ""
fullnameOverride: ""

# extra labels for the created resources
labels: {}

# Internal hostname
host: ""

# External DNS hostnames
hosts: []
